# Contrast Security RBAC Migration Configuration
# Copy this file to .env and fill in your actual values

# Contrast Security API Key (get from User Settings → Profile → API Key)
API_KEY=your_api_key_here

# Contrast Security server URL (include trailing slash)
BASE_URL=https://your-contrast-server.com/

# Base64 encoded authentication (username:service_key)
AUTH=your_base64_encoded_authorization_header

# Organization UUID (get from Organization Settings)
ORG=your_organization_uuid_here

# Directory for log files (default: Results)
LOG_DIR=Results